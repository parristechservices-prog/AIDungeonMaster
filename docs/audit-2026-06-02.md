---
doc: Deep-dive audit (mock path, API, prompts, gaps)
project: PartyQuest / AIDungeonMaster
date: 2026-06-02
audience: developers, coding agents
---

# Audit — 2026-06-02

> **Accuracy note:** This audit was written before and during the 2026-06-02 implementation pass. The "Priority fixes" section records items that shipped later that day. The gap matrix below has been updated to reflect the current code state after that pass.

Structured findings from a local play-test (no API keys), code review of `/api/turn` and prompts, and a line-by-line comparison against `ai-dm-app-gaps-and-next-steps.md`.

## Executive summary

The **hybrid architecture is sound** and the mock/table-rules path can complete a full one-shot. Several original risks were addressed on 2026-06-02: no-key LLM latency, the too-low per-turn request cap, appeal UI, mood-label honesty, and mock failure nudges. The main remaining risks are: **ephemeral production persistence**, **partial safety enforcement**, **non-seedable engine randomness**, **loose action economy**, and **prompt/eval drift**.

---

## Mock play-test (no `GROQ_API_KEY` / `OPENAI_API_KEY`)

Environment: `pnpm dev`, HTTP `POST /api/session/start` + `POST /api/turn`.

| Step | Expected | Observed |
|------|----------|----------|
| Session start | `social`, opening narration | OK |
| `hello` | Greeting, stay in social | OK — Mira greets |
| `I ask Mira about the courier` | Skill check + NPC update | **RNG-dependent** — on failure, stays in `social` with failure line |
| Retry social until pass | `exploration` | OK when roll ≥ DC |
| `I inspect the tracks` | Exploration check → `combat` on pass | OK |
| `I attack` ×2 | Engine combat, monsters to 0 HP | OK — matches Vitest regression with high rolls |

**Vitest regression** (`regression-flow.test.ts`) always passes because `Math.random` is mocked to `0.99`. **Live mock play** can stall on the first failed persuasion/perception roll until the player retries an “ask” or exploration phrase.

**Mock-only behaviors:**

- Fallback narration is prefixed with *"The wind shifts and the tale steadies itself."* (identifies table-rules mode; feels repetitive in-product).
- Mock does not set `ambient` (only LLM turns would).
- `what is my perception skill` (and similar) answers from state without engine rolls — good.

**Combat:** After one monster dies, the survivor can still attack on the next `monster_turn` — correct engine behavior.

---

## `/api/turn` pipeline

File: `src/app/api/turn/route.ts`

1. `getOrCreateSession(sessionId)`
2. `buildSystemPrompt(state)` + last **5** recaps appended
3. `generateDmTurn()` — configured chat-completions provider when credentials exist; otherwise returns `null` and the route uses mock/table-rules mode
4. On `null` → `deriveDmTurnFromInput()` (mock)
5. `runTurn()` → `validateNarrationAgainstState()` → `saveRecap` / dev log

**Strengths:** Clear fallback chain, recap memory in prompt, narration validator, structured dev logging.

**Issues found:**

| Issue | Severity | Detail |
|-------|----------|--------|
| Production persistence is ephemeral | High | Local sessions are saved under `.data/`; Vercel uses process memory plus client snapshot |
| No output safety classifier | Medium | Numeric validator logs warnings, but moderation/regeneration is not implemented |
| Monolithic prompt | Low | Design doc describes versioned prompt stack; current code uses one inline system prompt |
| AI outcome narration bridge | Medium | When AI sets `needsResultBeforeNarrating`, `runTurn` may append a mechanical summary rather than making a second narrator call |
| Non-seedable engine RNG | Medium | Engine rolls still use `Math.random()`, limiting deterministic replay |

---

## Prompts (`system-prompt.ts`, `provider.ts`)

**Strengths:**

- Engine owns numbers; explicit rules against inventing HP/AC/dice
- Canon log + NPCs injected each turn
- DM personas (`DM_PERSONAS`)
- Appeals section for `[APPEAL]` input
- Chat-completions `response_format: json_object`, `temperature: 0.1`, Zod validation on parse. Groq is the default provider; `PARTYQUEST_LLM_PROVIDER` can also specify a comma-separated provider failover list.

**Gaps:**

- Prompt is inline TypeScript — versioning convention in `dm-prompt-design.md` not enforced in code headers
- `CORE RESPONSE RULES` numbering jumps (5–10; no 1–4)
- No post-generation safety pass on narration
- No separate intent-classifier / adjudicator prompts (acceptable for V0, doc drift)

---

## Gaps doc vs codebase (`ai-dm-app-gaps-and-next-steps.md`)

| # | Topic | Status (2026-06-02) |
|---|--------|---------------------|
| 1 | Ambient audio | **Partial** — mood indicator UI, no audio files/playback |
| 2 | Appeal the DM | **Implemented for V0** — prompt support + play-screen button |
| 3 | Spatial combat | **Not started** — theatre of mind |
| 4 | Party split | N/A (solo) |
| 5 | Whispers / secrets | **Not started** |
| 6 | Accessibility | **Partial** — text-first; voice not shipped |
| 7 | Reconnect / AFK | **Partial** — `localStorage` snapshot; server state ephemeral on Vercel |
| 8 | Encounter balancing | **Partial** — simple CR/XP-style balancer and module auto-spawn exist; broader SRD import and stronger tests remain |
| 9 | Canon log / drift | **Strong** — `canonLog`, `add_canon_fact`, prompt injection |
| 10 | Cold-start UX | **Partial** — onboarding banner, choices, and tutorial scenario exist; failed-check onboarding still needs polish |
| 11 | Silence / pacing | **Not started** |
| 12 | Manual physical dice | **Implemented for core rolls** — skill checks, attacks, and death saves support manual d20 entry |
| 13 | DM persona | **Done** |
| 14 | Localisation | **Not started** |
| 15 | Analytics | **Partial** — dev logs only |
| 16 | Prompt versioning / eval | **Partial** — Vitest; no prompt files v0.x or eval harness |
| 17 | Cost caps | **Not started** |
| 18 | Host succession | N/A |
| 19–20 | Crafting / house rules | **Not started** |

**Part 2 flaws — addressed well:** narrow engine scope, pre-authored scenarios, hybrid JSON contract, regression tests.

**Still open:** safety as code, mid-session Session 0 panel, cost model/caps, voice pipeline (future).

---

## Doc accuracy issues

Older docs previously overstated some partial features. `docs/TEN_IMPROVEMENTS.md` and `docs/todo.md` have been corrected to distinguish implemented, partial, and planned work.

---

## Priority fixes (Phase A — shipped 2026-06-02)

See `docs/full-improvement-plan.md` for the full roadmap.

| Fix | Status |
|-----|--------|
| Skip LLM HTTP when no API keys (`hasLlmCredentials`, `PARTYQUEST_FORCE_MOCK`) | Done |
| Per-turn engine request cap = 5 | Done |
| Mock failure hints + social/exploration nudges | Done |
| Appeal button on play screen | Done |
| Scene mood indicator (honest labeling) | Done |
| Mock flavor prefix opt-in (`PARTYQUEST_MOCK_FLAVOR=true`) | Done |

---

## How to reproduce this audit

```bash
# No API keys
pnpm dev

# HTTP play-test (Node)
node -e "
const base='http://localhost:3000';
const sid='audit-'+Date.now();
const post=(p,b)=>fetch(base+p,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(b)}).then(r=>r.json());
(async()=>{
  await post('/api/session/start',{sessionId:sid,adventureId:'brindlehook-inn',characterId:'fighter'});
  for (const t of ['hello','I ask Mira about the courier']) {
    const r=await post('/api/turn',{sessionId:sid,playerInput:t});
    console.log(t,r.sceneId,r.fallbackUsed,(r.engineResults||[]).map(e=>e.kind+':'+e.ok));
  }
})();
"

pnpm test
```
